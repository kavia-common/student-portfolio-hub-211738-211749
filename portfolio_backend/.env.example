# Optional override for database DSN (recommended in deployments).
# If not set, backend will attempt to read DSN guidance from:
#   ../student-portfolio-hub-211738-211748/database/db_connection.txt
#
# IMPORTANT: SQLAlchemy async requires postgresql+asyncpg:// scheme.
DATABASE_DSN=postgresql+asyncpg://appuser:dbuser123@localhost:5000/myapp
